#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
		int a, A;

		for (a = 'a'; a <= 'z'; a++)
		{
			putchar(0);
		}
		for (A = 'A'; A <= "Z"; A++)
		{
			putchar(0);
	}
		putchar('\n');
		return (0);
}
